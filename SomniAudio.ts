export function startBedtime(voicePath: string, deltaPath: string): void {
  console.log('[Somni] startBedtime stub called', voicePath, deltaPath);
}

export function startMorning(voicePath: string): void {
  console.log('[Somni] startMorning stub called', voicePath);
}

export function stop(): void {
  console.log('[Somni] stop stub called');
}
